version: '3.9'

services:
  backend:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - db
  
  db:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABSE: testdb
